The program can be run from the Main class. 
If running from the command line: java -jar Assignment3.jar
The program runs all of the tests at once, so you may need to increase the heap size in order for it to complete. "Solve the knapsack problem with greedy, dynamic programming and graph search" 
